{
  "definitions": [
    {
      "title": "Filters",
      "id": "filters",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Filters",
              "model": "filters",
              "filter": "filters"
            }
          }
        }
      }
    },
    {
      "title": "Filter",
      "id": "filter",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block/item",
            "template": {
              "name": "Filter",
              "model": "filter"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "filter",
      "fields": [
        {
          "component": "text",
          "valueType": "string",
          "name": "name",
          "label": "Filter Name",
          "description": "The filter dimension name (e.g. Year, Topic)"
        },
        {
          "component": "text",
          "valueType": "string",
          "name": "values",
          "label": "Filter Values",
          "description": "Comma-separated list of filter options (e.g. 2024, 2023, 2022)"
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "filters",
      "components": [
        "filter"
      ]
    }
  ]
}
